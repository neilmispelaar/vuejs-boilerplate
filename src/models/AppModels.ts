/**
 * Vuex State Model
 */
export interface RootState {

  // Flag to show if the data has been loaded
  dataLoaded: Boolean;
  dataError: Boolean;
  // data: Array<TYPE>;

}

/**
 * Thing Model
 */
// export interface Thing {
//   id: string;
// }
